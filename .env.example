# URL del backend (opcional en desarrollo, necesario en producción)
# En desarrollo Vite proxy automáticamente /api -> localhost:3000
# En producción (Vercel) apunta a la URL del backend desplegado
# Ejemplo: https://slott-back.herokuapp.com
VITE_API_URL=
